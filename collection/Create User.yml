info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/users
  body:
    type: json
    data: |-
      {
        "fullName": "Thiago Correia",
        "address": "José Braga Street",
        "number": "10",
        "complement": "House 7"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
